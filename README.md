Lightweight and resources-limited but yet effective version

Env vars
```
WEB_PORT
```

I generated assets from svg with `bun x pwa-asset-generator logo.svg ./output -i ./index.html -m ./manifest.json -e false -o false -a / -f -w -q 100`
