import ActionFetch__Featured from "@/actions/ActionFetch__Featured";
import { createQuery } from "@/utils/query";

const useQueryEntry__Featured = createQuery(() => ActionFetch__Featured());
export default useQueryEntry__Featured;
