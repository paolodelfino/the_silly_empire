TODO: Better decide? Maybe leave all allowing for combinations
If two components have the exact same query but with different params:
  1. use another "__<specifier>" (you can also use component's name)
  2. place it in component's folder
  3. use "copy[ <n>]"
  
TODO: Write spec